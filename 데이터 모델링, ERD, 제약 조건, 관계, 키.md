# 데이터 모델링

- 현실 세계에 존재하는 데이터를 컴퓨터 세계의 데이터 베이스로 옮기는 변환 과정
- 정보시스템 구축의 대상이 되는 업무 내용을 분석하여 이해하고 약속된 표시법에 의해 표현하는 것
    - 분석된 모델을 가지고 실제 데이터베이스를 생성하여 개발 및 데이터 관리에 사용
    - 데이터를 추상화한 데이터 모델은 데이터베이스의 골격을 이해하고 그 이해를 바탕으로 SQL문장을 기능, 성능적인 측면에서 효과적으로 작성 가능

## 데이터 모델링 순서 절차

### 1. 업무 파악 (요구사항 수집 및 분석)

- 요구사항 분석
- 업무 기술서 (요구사항 정의서) 확인
- 설계 보조 요소:
    - 용어 사전
        - 같은 말을 다르게 쓰지 않도록 용어 기준을 맞추는 문서
    - 도메인 정의
        - 속성의 값 범위, 형식, 공통 규칙을 정리하는 기준

### 2. 개념 데이터 모델링

- 작업과 데이터 간의 관계를 구상하는 단계
- ERD 다이어그램 생성 → 각 개체들의 관계 발견 및 표현
    - entity, attribute, relation

### 3. 논리 데이터 모델링

- 구체화된 업무 중심의 데이터 모델을 만드는 과정
- 정보 공학 표기법을 사용해서 테이블 형태로 재구성
- 업무에 대한 Key, 속성, 관계 표시
- 정규화 → 데이터 모델 일관성 확보 및 중복 제거를 통해 신뢰성 있는 데이터 구조 획득

### 4. 물리 데이터 모델링

- 데이터 베이스 선택 → 실제 테이블을 만드는 작업

---

## ERD (Entity Relationship Diagram)

- 개체 관계 다이어그램
- 요구 분석 사항에서 얻은 엔티티와 속성들의 관계를 그림으로 표현한 것
- 피터첸 표기법
- `ERwin`, `ERDCloud`, `EXERD` 사용

### ERD 구성 요소

#### 1. 엔티티 (Entity)

- 정의 가능한 사물, 개념
- 테이블
- ex) 유형, 무형, 문서, 이력, 코드

#### 2. 속성 (Attribute)

- 개체가 갖고있는 속성
- 테이블의 각 필드(컬럼)
- 종류
    - 기초속성: 원래 가지고 있는 속성, 현업에서 기본적으로 사용되는 속성
    - 추출 속성: 시초 속성으로부터 가공처리(계산)을 통해 얻어질 수 있는 속성 (자료의 중복성 및 무결성 확보를 위해 최소화)
    - 설계 속성: 실제로 존재하지는 않으나 시스템의 효율성을 도모하기 위해 설계자가 임의로 부여하는 속성

#### 3. 관계 (Relation)

### 피터첸 표기법 (Peter Chen Notation) ↔ IE 방식

![image.png](attachment:77261291-1763-43ad-861f-39674a084e6a:image.png)

---

## 제약 조건

### Primary Key

- 테이블에서 각 행을 유일하게 구분하는 키
- 중복, NULL 불가

### Foreign Key

- 다른 테이블의 PK 값을 참조해야 하는 규칙
- 관계 무결성 보장
    - 존재하는 값만 참조 가능

### UNIQUE

- 값의 중복을 막음
- NULL 허용

### NOT NULL

- 반드시 값이 있어야 하는 칼럼

### CHECK

- 특정 조건만 허용

```sql
CHECK (급여 >= 0)
CHECK (성별 IN ('M', 'F'))
```

---

## 관계 (Relationship)

- 엔티티 간 데이터  종속성과 참여 규칙을 표현하는 것
- Crow’s Foot 표기법 사용
    - optional one( **o|** ): 0 또는 1로 관계에 참여
    - mandatory one( **||** ): 반드시 1개로 관계에 참여
    - optional many( **o{** ): 0이상으로 관계에 참여
    - mandatory many( **|{** ): 1이상으로 관계에 참여

### 1. Cardinality 관계

- 엔터티 간 몇 개 까지 연결 가능한가를 표현
- 1:1  (One To One)
    - 한 엔터티가 다른 엔터티 하나와만 연결
    - 거의 같은 생명주기
    - 컬럼 분리 목적
- 1:N (One To Many)
    - 부모 하나가 자식 여러 개를 가짐
- N:M (Many To Many)
    - 양쪽 모두 여러 개 연결 가능
    - 실제 DB에서는 직접 구현 불가 → 반드시 연결 엔터티 필요
        
        ex) 학생, 강의, 수강 관계에서 수강 테이블 생성 시 학생, 강의와 각각 1:N 관계
        

### 2. 일반화, 특수화 관계

- 상속 개념
- 슈퍼타입1: 1 서브타입 형태로 연결
- 슈퍼타입(Supertype)
    - 공통 속성을 가지는 상위 엔티티
- 서브타입(Subtype)
    - 슈퍼타입의 속성을 상속받고 추가 속성을 가짐

### 3. 배타 관계 (Exclusive)

- XOR 관계
    - 둘 중 하나만 가능
    - 동시에 여러 관계 참여 불가

### 4. 포함 관계 (Inclusive)

- 배타 관계 반대
- 동시에 여러 관계 참여 가능
    
    ex) 한 사람이 학생일 수도 있고 직원일 수도 있음
    

### 5. 식별 관계 (Identifying Relationship)

- 부모 PK가 자식 PK 일부가 됨
    - 부모 없으면 자식 존재 불가
    - 강한 종속
    - 복합키 자주 발생
    - 실선 사용

```sql
주문
(PK) 주문ID

주문상세
(PK)(FK) 주문ID
(PK) 상품ID
```

### 6. 비식별 관계 (Non-Identifying)

- 부모 PK를 FK로만 참조
    - 자식 독립 PK 존재
    - 가장 흔함
    - 점선 사용

```sql
부서
(PK) 부서ID

사원
(PK) 사원ID
(FK) 부서ID
```

### 7. Optional 관계

- 필수 관계
    - 반드시 필요
- 선택 관계
    - 없어도 됨

### 8. 재귀 관계(Recursive Relationship)

- 자기 자신 참조
- 같은 테이블에 FK 존재

---

## 키 (Key)

- 데이터를 식별하거나 테이블 간 관계를 연결하기 위한 컬럼

| 키 | 의미 |
| --- | --- |
| PK | 기본 식별 키 |
| FK | 다른 테이블 참조 |
| 후보키 | PK 후보 |
| 대체키 | 선택 안 된 후보키 |
| 슈퍼키 | 식별 가능한 키 |
| 복합키 | 여러 컬럼 조합 |
| 자연키 | 업무 의미 키 |
| 대리키 | 인위적 키 |
| UNIQUE | 중복 방지 |
- 복합키 순서
    - 복합키: 칼럼 여러 개를 묶어서 하나의 키로 사용하는 것
    - 성능과 연관 있음
        - 앞 컬럼 활용도가 중요
        - DB 인덱스는 보통 B-Tree 구조 → 앞 컬럼부터 정렬하기 때문
    1. 현업에서 많이 쓰는 컬럼 먼저
        - 자주 조회되는 컬럼을 앞에 둠
    2. WHERE 조건에 많이 쓰는 컬럼 먼저
    3. Equal(=) 조건 컬럼 우선
        - B-Tree 인덱스는 = 조건 까지는 강하게 탐색 가능
        - 범위 조건(>, <, BETWEEN) 이후부터 효율 급감
    4. 분포도가 작은 것 (중복 적은 것)
        - 선택도: 얼마나 데이터를 잘 좁혀주는가? → DB가 인덱스 쓸지 풀스캔 할지 선택
